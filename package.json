{
    "name": "my-cypress-mock-backend",
    "version": "1.0.0",
    "scripts": {
        "start": "json-server --watch db.json --port 5000"
    },
    "devDependencies": {
        "cypress-mochawesome-reporter": "^3.8.2",
        "cypress-xpath": "^2.0.1",
        "json-server": "^0.17.4",
        "mochawesome": "^7.1.3",
        "mochawesome-merge": "^5.0.0",
        "mochawesome-report-generator": "^6.2.0"
    }
}
